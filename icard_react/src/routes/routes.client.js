import {clientlayout} from '../layout';
import {Home} from '../pages/clientpage';

const routesClient = [
    {
        path: "/",
        layout: clientlayout,
        component: Home,
        
    },


];

export default routesClient;